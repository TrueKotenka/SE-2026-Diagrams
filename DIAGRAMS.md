# Модели системы учёта дефектов и поведения микроволновой печи


## 1. Диаграмма прецедентов (Use Case)
Описывает пользователей и случаи использования разрабатываемого приложения.
```mermaid
flowchart LR
  %% Акторы (пользователи)
  Dispatcher(["Диспетчер"])
  Foreman(["Бригадир"])
  Worker(["Ремонтник"])
  QA(["Отдел контроля качества"])
  Manager(["Начальник цеха"])
  Accounting(["Бухгалтерия"])

  %% Система и прецеденты
  subgraph "Система учёта дефектов"
    UC1("Внести данные о зонах, местах и бригадах")
    UC2("Зарегистрировать дефект")
    UC3("Направить авто в свободную зону")
    UC4("Назначить ремонт рабочему")
    UC5("Зарегистрироваться доступным в начале смены")
    UC6("Зафиксировать время начала и конца ремонта")
    UC7("Получить сводный отчёт по участкам")
    UC8("Получить отчёт об общем кол-ве ремонтов")
    UC9("Получить информацию по ремонтникам бригады")
    UC10("Получить отчёт о количестве отработанных смен")
  end

  %% Связи
  Dispatcher --> UC1
  Dispatcher --> UC2
  Dispatcher --> UC3

  Foreman --> UC4
  Foreman --> UC9

  Worker --> UC5
  Worker --> UC6

  QA --> UC7
  Manager --> UC8
  Accounting --> UC10

```

**Пояснение:**

* Система должна обладать возможностью вносить данные о ремонтных зонах, местах и бригадах.
* Диспетчер должен иметь возможность видеть свободные места и направлять автомобили для ремонта в свободную ремонтную зону.
* Бригадир имеет возможность назначить ремонт конкретному рабочему.
* Рабочий может зарегистрироваться в системе как доступный и зафиксировать время начала и конца ремонта.
* Различные отделы получают специфические отчёты: отдел контроля качества получает сводный отчёт по участкам, начальник цеха — отчёт об общем количестве ремонтов, бригадир — статистику по своей бригаде, а бухгалтерия — отчёт об отработанных сменах.

---

## 2. Диаграмма активностей (Activity Diagram)

Описывает основной бизнес-процесс, поддерживаемый приложением — регистрацию и ремонт дефекта.

```mermaid
flowchart TD
  start((Начало)) --> A

  subgraph QA1 ["Отдел контроля качества"]
    A(Выявление дефекта сборки) --> B(Снятие автомобиля с конвейера)
  end

  subgraph Disp ["Диспетчер"]
    B --> C(Просмотр свободных мест в ремонтных зонах)
    C --> D(Направление автомобиля в свободную ремонтную зону)
  end

  subgraph Worker1 ["Ремонтник (Диагност)"]
    D --> E(Поступление автомобиля в ремонтную зону)
    E --> F(Выполнение первоначальной диагностики)
    F --> G(Регистрация дефекта в системе)
  end

  subgraph Boss ["Бригадир"]
    G --> H(Назначение ремонта доступному рабочему)
  end

  subgraph Worker2 ["Ремонтник (Исполнитель)"]
    H --> I(Фиксация времени начала ремонта)
    I --> J(Устранение дефекта)
    J --> K(Фиксация времени конца ремонта)
  end

  subgraph QA2 ["Отдел контроля качества"]
    K --> L(Возврат автомобиля в зону тестирования или на конвейер)
  end
  
  L --> stop((Конец))

```

**Пояснение:**

* В случае обнаружения дефекта автомобиль снимается с конвейера и отправляется в ремонтную зону.
* Диспетчер направляет автомобиль для ремонта в свободную ремонтную зону.
* Дефект регистрируется после поступления автомобиля в ремонтную зону и выполнения первоначальной диагностики.
* После устранения дефекта автомобиль возвращается на конвейер либо в зону тестирования.

---

## 3. BPMN-диаграмма (Бизнес-процесс завода)

BPMN-диаграмма для всего бизнес-процесса завода, включая внешних его участников.

```mermaid
flowchart TD
  start((Начало)) --> A

  subgraph Dealer1 ["Дилер"]
    A(Отправка набора заказов на автомобили)
  end

  subgraph Plan ["Отдел планирования"]
    A --> B(Составление производственного плана)
  end

  subgraph Prod ["Производство"]
    B --> C(Сборка кузова)
    C --> D(Покраска - несколько дней)
    D --> E(Сборка остальных комплектующих на конвейере)
  end

  subgraph QA ["Отдел контроля качества"]
    E --> F(Стендовые и ходовые испытания)
    F --> G{Выявлены дефекты?}
  end

  subgraph Repair ["Ремонтная зона"]
    G -- Да --> H(Снятие с конвейера и отправка в ремонт)
    H --> I(Устранение дефекта)
    I --> F
  end

  subgraph Dealer2 ["Дилер"]
    G -- Нет --> J(Отгрузка автомобиля заказчику)
  end
  
  J --> stop((Конец))

```

**Пояснение:**

* Завод получает набор заказов от дилеров.
* Отдел планирования составляет производственный план.
* Процесс производства включает сборку кузова, покраску и сборку остальных комплектующих.
* В отделе контроля качества автомобиль проходит серию стендовых и ходовых испытаний.
* В случае успешного прохождения проверок автомобиль отгружается заказчику.

---

## 4. Диаграмма классов (Class Diagram)

Диаграмма классов, моделирующая данные, хранимые системой.

```mermaid
classDiagram
  class Автомобиль {
    +String vin
    +String комплектация
    +String цвет
    +Date датаПоступления
  }
  class Дефект {
    +String местоНаСхеме
    +String возможнаяПричина
  }
  class Ремонт {
    +DateTime времяНачала
    +DateTime времяКонца
  }
  class РемонтнаяЗона {
    +String названиеУчастка
  }
  class РемонтноеМесто {
    +Integer номерМеста
    +Boolean статусСвободно
  }
  class Бригада {
    +Integer номерБригады
    +String смена
  }
  class Сотрудник {
    +String фио
    +String роль
    +Boolean статусДоступен
  }

  Автомобиль "1" --> "*" Дефект : имеет
  Дефект "1" --> "1" Ремонт : требует
  РемонтнаяЗона "1" *-- "1..4" РемонтноеМесто : содержит
  РемонтнаяЗона "*" --> "1" Бригада : обслуживается
  Бригада "1" *-- "*" Сотрудник : состоит из
  Сотрудник "1" --> "*" Ремонт : выполняет
  Ремонт "*" --> "1" РемонтноеМесто : проводится на

```

**Пояснение:**

* Система хранит данные о ремонтных зонах, ремонтных местах, ремонтных бригадах и их бригадирах.
* Каждый участок конвейера имеет несколько (от одной до четырёх) ремонтных зон, в каждой зоне имеется несколько ремонтных мест.
* Каждая ремонтная зона обслуживается ремонтной бригадой, имеющей бригадира и нескольких ремонтников.
* При регистрации дефекта указывается место дефекта на схеме, возможная причина, номер автомобиля, дата поступления и выполнявший диагностику ремонтник.

---

## 5. Диаграмма компонентов (Component Diagram)

Диаграмма компонентов требуемой системы.

```mermaid
flowchart TD
  subgraph System ["Система учёта дефектов"]
    WebUI["Web-интерфейс (Диспетчер/Отчёты)"]
    MobileUI["Мобильный интерфейс (Ремонтники/Бригадир)"]
    
    subgraph CoreContainer ["Core (Бизнес-логика)"]
      API("REST API")
    end
    
    Reports["Модуль генерации отчётов"]
    DB[("База данных")]
  end

  WebUI --> API
  MobileUI --> API
  API --> CoreContainer
  CoreContainer --> DB
  CoreContainer --> Reports
  Reports --> DB

```

---

## 6. Диаграмма развёртывания (Deployment Diagram)

Диаграмма развёртывания с указанием компонентов, разворачиваемых на узле.

```mermaid
flowchart TD
  subgraph AppServerNode ["Сервер Приложений (Application Server)"]
    Core["DefectTracker_Core.jar"]
    Rep["Reporting_Engine.jar"]
  end

  subgraph DBServerNode ["Сервер Баз Данных (Database Server)"]
    DB[("PostgreSQL")]
  end

  subgraph PCNode ["Рабочая станция диспетчера/руководства"]
    WebBrowser["Браузер (Web-клиент)"]
  end

  subgraph TabletNode ["Планшет ремонтника/бригадира"]
    App["Мобильное приложение (Android/iOS)"]
  end

  WebBrowser -- HTTP/HTTPS --> AppServerNode
  App -- HTTP/HTTPS --> AppServerNode
  AppServerNode -- JDBC/TCP --> DBServerNode

```

---

## 7. Диаграмма последовательностей (Sequence Diagram)

Диаграмма последовательностей регистрации и ремонта дефекта.

```mermaid
sequenceDiagram
  actor Disp as Диспетчер
  participant Sys as ИнформационнаяСистема
  actor Boss as Бригадир
  actor Worker as Ремонтник

  Disp->>Sys: направитьАвтоВРемонт(НомерАвто, ID_Зоны)
  Sys-->>Disp: подтверждение
  Disp->>Sys: зарегистрироватьДефект(Место, Причина)
  Sys-->>Boss: уведомлениеОДефекте
  Boss->>Sys: назначитьРемонт(ID_Дефекта, ID_Ремонтника)
  Sys-->>Worker: задачаНаРемонт
  
  Worker->>Sys: зафиксироватьВремяНачала()
  activate Sys
  Worker->>Sys: зафиксироватьВремяКонца()
  deactivate Sys
  
  Sys-->>Disp: статусИзменен(РемонтОкончен)

```

---

## 8. Диаграмма конечных автоматов (State Machine Diagram)

Диаграмма конечных автоматов, описывающая поведение микроволновки.

```mermaid
stateDiagram-v2
  [*] --> Ожидание

  Ожидание --> ДверцаОткрыта : открыть дверцу
  ДверцаОткрыта --> Ожидание : закрыть дверцу

  Ожидание --> Нагрев : нажать Старт
  Нагрев --> Пауза : нажать Стоп / открыть дверцу
  Пауза --> Нагрев : закрыть дверцу и нажать Старт
  Пауза --> Ожидание : нажать Стоп (сброс программы)

  Нагрев --> Ожидание : таймер истек

```

---

## 9. Временная диаграмма (Timing Diagram)

Временная диаграмма любого сценария работы микроволновки.

```mermaid
gantt
  title Временная диаграмма работы микроволновки
  %% Используем unix timestamp (секунды) для относительного отсчета времени
  dateFormat X
  axisFormat %s

  section Дверца
  Закрыта               :0, 5
  Открыта               :5, 10
  Закрыта               :10, 45
  Открыта               :45, 50

  section Магнетрон (Нагрев)
  Выкл                  :0, 12
  Вкл                   :12, 42
  Выкл                  :42, 50

  section Таймер
  0                     :0, 10
  Установлен (30s)      :10, 12
  Отсчет                :12, 42
  0                     :42, 50

```
